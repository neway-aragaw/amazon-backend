name: Run Tests and Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

<<<<<<< HEAD
      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Run ESLint (non-blocking)
        run: |
          npm run lint || true
=======
      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Run Linter
        run: npm run lint
>>>>>>> af76e84 (Add GitHub Actions CI workflow)

      - name: Run Tests
        run: npm test
